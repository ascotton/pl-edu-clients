import { TestBed, waitForAsync } from '@angular/core/testing';

import { PLClientService } from './pl-client.service';

describe('PLClientService', () => {
    describe('formatClient', () => { 
        beforeEach(() => {
            // TODO
        });

        it('should do something', () => {
            expect(true).toBe(true);
        });
    });
});


